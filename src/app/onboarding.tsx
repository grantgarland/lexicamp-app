import { OnboardingScreen } from '@/screens/OnboardingScreen';

// First-run narrative arc (O-01…O-09). Hands off to /auth on completion.
export default function Onboarding() {
  return <OnboardingScreen />;
}
