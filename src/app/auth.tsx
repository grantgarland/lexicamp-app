import { AuthScreen } from '@/screens/AuthScreen';

// Sign up / sign in (O-10 / O-11). Entered from onboarding; routes into the app on success.
export default function Auth() {
  return <AuthScreen />;
}
